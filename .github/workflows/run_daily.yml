name: Daily Auto Commit

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write 
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set dynamic branch name
      run: |
        
        START_SEC=$(TZ="Europe/Tallinn" date -d "2026-05-23 00:00:00" +%s)
        CURRENT_SEC=$(TZ="Europe/Tallinn" date +%s)
        DIFF_SEC=$(( CURRENT_SEC - START_SEC ))
        DAYS=$(( DIFF_SEC / 86400 ))
        WEEK=$(( (DAYS / 7) + 1 ))
        if [ $WEEK -gt 4 ]; then WEEK=4; fi
        if [ $WEEK -lt 1 ]; then WEEK=1; fi
        echo "BRANCH_NAME=week$WEEK" >> $GITHUB_ENV

  
    - name: Configure Git Author
      run: |
        git config --global user.name "SofijaBeljakova"
        git config --global user.email "sofija.beljakova@tptlive.ee"

    - name: Run script
      run: python auto_commit.py

    - name: Commit and Push changes
      run: |
        if [ ! -f README.md ]; then
          touch README.md
        fi
        
        
        git checkout $BRANCH_NAME || git checkout -b $BRANCH_NAME
        
        git add README.md
        
        
        git commit -m "Päevane uuendus ($BRANCH_NAME)" || echo "No changes to commit"
        
        
        git push origin HEAD:$BRANCH_NAME
